{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat application with MERN stack and Socket.IO",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start:backend": "npm run start:prod --prefix backend",
    "start:frontend": "npx serve -s frontend/dist",
    "dev:backend": "npm run start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "preview:frontend": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": ["chat", "mern", "socket.io", "react", "node"],
  "author": "Your Name",
  "license": "ISC"
}